# goplatoon
Splatoon stats list. For kotaoue's personal use.

## Usage
```
# メイン一覧
go run main.go --mode=main

# サブ一覧
go run main.go --mode=sub

# スペシャル一覧
go run main.go --mode=sp

# ステージ一覧
go run main.go --mode=stage
```
