# goplatoon
Splatoon stats list. For kotaoue's personal use.

## Usage
```
# メイン一覧
go run main.go --target=main

# サブ一覧
go run main.go --target=sub

# スペシャル一覧
go run main.go --target=sp

# ステージ一覧
go run main.go --target=stage
```
